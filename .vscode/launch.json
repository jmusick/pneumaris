{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Pneumaris: Local Dev",
      "type": "firefox",
      "request": "launch",
      "reAttach": true,
      "url": "http://localhost:4321/",
      "webRoot": "${workspaceFolder}",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "Pneumaris: dev"
    }
  ]
}
